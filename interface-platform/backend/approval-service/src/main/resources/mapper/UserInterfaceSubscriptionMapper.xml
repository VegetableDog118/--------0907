<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.powertrading.approval.mapper.UserInterfaceSubscriptionMapper">

    <!-- 批量插入订阅记录 -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO user_interface_subscriptions (
            id, user_id, interface_id, application_id, status,
            subscribe_time, expire_time, call_count, create_time, update_time
        ) VALUES
        <foreach collection="subscriptions" item="item" separator=",">
            (
                #{item.id}, #{item.userId}, #{item.interfaceId}, #{item.applicationId}, #{item.status},
                #{item.subscribeTime}, #{item.expireTime}, #{item.callCount}, #{item.createTime}, #{item.updateTime}
            )
        </foreach>
    </insert>

</mapper>